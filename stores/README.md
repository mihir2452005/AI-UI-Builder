# Stores

This directory contains Zustand state management stores:

- `canvas-store.ts` - Canvas state (UI document, selection, viewport)
- `project-store.ts` - Project management state
- `ui-store.ts` - UI state (modals, panels, sidebar)
